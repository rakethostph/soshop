<%= form_with(model: sub_category, local: true) do |form| %>
  <% if sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
      <% sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :subcategory_name %>
    <%= form.text_field :subcategory_name %>
  </div>

  <div class="field">
    <%= form.label :sub_category_url %>
    <%= form.text_field :sub_category_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
